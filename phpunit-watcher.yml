watch:
    directories:
        - src
        - test
    fileMask: '*.php'
notifications:
    passingTests: false
    failingTests: false
phpunit:
    binaryPath: vendor/bin/phpunit
    arguments: '--stop-on-error --stop-on-failure --testdox'
    timeout: 20
